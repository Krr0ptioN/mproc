#pragma once

#include "stdc++.hpp"


namespace mproc {

    namespace dirs {
        // System information from /proc/version
        // kernel version 
        // OS name
        // System Architecture name
        const char* proc_version = "/proc/version";
    }
}
